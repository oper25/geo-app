apiVersion: v1
kind: Service
metadata:
  name: {{ include "geo-app.fullname" . }}
  labels:
    {{- include "geo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "geo-app.selectorLabels" . | nindent 4 }}
